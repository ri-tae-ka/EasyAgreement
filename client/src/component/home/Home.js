import React, { Fragment } from "react";
import Metadata from "../layout/Metadata";

const Home = () => {
  return (
    <Fragment>
      <Metadata title="EasyAgreement" />
      <div>Home</div>
    </Fragment>
  );
};

export default Home;
